/*
 * Exercise 11.35: In buildMap, what effect, if any, would there be from
 * rewriting
 *     trans_map[key] = value.substr(1);
 * as trans_map.insert({key, value.substr(1)})?
 */

int main() { return 0; }

/*
 * $ g++ -o main chapter-11/exercise-35.cc && ./main

 */
