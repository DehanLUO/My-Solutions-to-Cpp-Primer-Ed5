/*
 * Exercise 11.34: What would happen if we used the subscript operator instead
 * of find in the transform function?
 */

int main() { return 0; }

/*
 * $ g++ -o main chapter-11/exercise-34.cc && ./main

 */
