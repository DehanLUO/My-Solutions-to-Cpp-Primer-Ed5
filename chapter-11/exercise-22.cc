/*
 * Exercise 11.22: Given a map<string, vector<int>>, write the types used as an
 * argument and as the return value for the version of insert that inserts one
 * element.
 */

int main() { return 0; }

/*
 * $ g++ -o main chapter-11/exercise-22.cc && ./main

 */
