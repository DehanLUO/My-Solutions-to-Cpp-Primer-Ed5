/*
 * Exercise 15.10: Recalling the discussion from § 8.1 (p. 311), explain how the
 * program on page 317 that passed an ifstream to the Sales_data read function
 * works.
 */

int main() { return 0; }

/*
 * $ g++ -o main chapter-15/exercise-10.cc && ./main

 */
