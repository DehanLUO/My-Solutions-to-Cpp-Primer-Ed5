/*
 * Exercise 14.14: Why do you think it is more efficient to define operator+ to
 * call operator+= rather than the other way around?
 */

int main() { return 0; }

/*
 * $ g++ -o main chapter-14/exercise-14.cc && ./main

 */
