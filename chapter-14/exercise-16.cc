/*
 * Exercise 14.16: Define equality and inequality operators for your StrBlob
 * (§ 12.1.1, p. 456), StrBlobPtr (§ 12.1.6, p. 474), StrVec (§ 13.5, p. 526),
 * and String (§ 13.5, p. 531) classes.
 */

int main() { return 0; }

/*
 * $ g++ -o main chapter-14/exercise-16.cc && ./main

 */
