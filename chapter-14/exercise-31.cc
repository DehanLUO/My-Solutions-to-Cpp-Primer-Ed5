/*
 * Exercise 14.31: Our StrBlobPtr class does not define the copy constructor,
 * assignment operator, or a destructor. Why is that okay?
 */

int main() { return 0; }

/*
 * $ g++ -o main chapter-14/exercise-31.cc && ./main

 */
