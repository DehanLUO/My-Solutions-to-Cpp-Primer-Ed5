/*
 * Exercise 13.41: Why did we use postfix increment in the call to construct
 * inside push_back? What would happen if it used the prefix increment?
 */

int main() { return 0; }

/*
 * $ g++ -o main chapter-13/exercise-41.cc && ./main

 */
