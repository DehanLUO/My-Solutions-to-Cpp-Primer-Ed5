/*
 * Exercise 13.33: Why is the parameter to the save and remove members of
 * Message a Folder&? Why didn’t we define that parameter as Folder? Or const
 * Folder&?
 */

int main() { return 0; }

/*
 * $ g++ -o main chapter-13/exercise-33.cc && ./main

 */
