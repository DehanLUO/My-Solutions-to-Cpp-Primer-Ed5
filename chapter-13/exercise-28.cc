/*
 * Exercise 13.28: Given the following classes, implement a default constructor
 * and the necessary copy-control members.
 *     (a) class TreeNode {
 *          private:
 *           std::string value;
 *           int count;
 *           TreeNode *left;
 *           TreeNode *right;
 *         };
 *
 *     (b) class BinStrTree {
 *          private:
 *           TreeNode *root;
 *         };
 */

int main() { return 0; }

/*
 * $ g++ -o main chapter-13/exercise-28.cc && ./main

 */
