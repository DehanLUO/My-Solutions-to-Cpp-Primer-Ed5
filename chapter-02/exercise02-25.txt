/*
 * Exercise 2.25: Determine the types and values of each of the following
 * variables.
 *     (a) int* ip, i, &r = i;  (b) int i, *ip = 0;  (c) int* ip, ip2;
 */

/*
 * (a) int* ip, i, &r = i;
 * `ip` is a pointer to an int. i` is an int. r is a reference to i;
 *
 * (b) int i, *ip = 0;
 * `i` is an int.
 * `ip`, which is initialized to  a null pointer, is a pointer to int.
 *
 * (c) int* ip, ip2;
 * `ip` is a pointer to int. `ip2` is an int.
 */
