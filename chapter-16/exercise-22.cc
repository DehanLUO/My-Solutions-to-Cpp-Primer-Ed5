/*
 * Exercise 16.22: Revise your TextQuery programs from § 12.3 (p. 484) so that
 * the shared_ptr members use a DebugDelete as their deleter (§ 12.1.4, p. 468).
 */

int main() { return 0; }

/*
 * $ g++ -o main chapter-16/exercise-22.cc && ./main

 */
