/*
 * Exercise 16.42: Determine the type of T and of val in each of the following
 * calls:
 *     template <typename T> void g(T&& val);
 *     int i = 0; const int ci = i;
 *     (a) g(i);         (b) g(ci);        (c) g(i * ci);
 */

int main() { return 0; }

/*
 * $ g++ -o main chapter-16/exercise-42.cc && ./main

 */
