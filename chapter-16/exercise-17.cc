/*
 * Exercise 16.17: What, if any, are the differences between a type parameter
 * that is declared as a typename and one that is declared as a class? When must
 * typename be used?
 */

int main() { return 0; }

/*
 * $ g++ -o main chapter-16/exercise-17.cc && ./main

 */
