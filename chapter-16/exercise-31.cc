/*
 * Exercise 16.31: Explain how the compiler might inline the call to the deleter
 * if we used DebugDelete with unique_ptr.
 */

int main() { return 0; }

/*
 * $ g++ -o main chapter-16/exercise-31.cc && ./main

 */
