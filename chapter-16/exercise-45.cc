/*
 * Exercise 16.45: Given the following template, explain what happens if we call
 * g on a literal value such as 42. What if we call g on a variable of type int?
 *     template <typename T> void g(T&& val) { vector<T> v; }
 */

int main() { return 0; }

/*
 * $ g++ -o main chapter-16/exercise-45.cc && ./main

 */
