/*
 * Exercise 16.26: Assuming NoDefault is a class that does not have a default
 * constructor, can we explicitly instantiate vector<NoDefault>? If not, why
 * not?
 */

int main() { return 0; }

/*
 * $ g++ -o main chapter-16/exercise-26.cc && ./main

 */
