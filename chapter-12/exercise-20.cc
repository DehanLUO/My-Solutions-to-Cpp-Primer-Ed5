/*
 * Exercise 12.20: Write a program that reads an input file a line at a time
 * into a StrBlob and uses a StrBlobPtr to print each element in that StrBlob.
 */

int main() { return 0; }

/*
 * $ g++ -o main chapter-12/exercise-20.cc && ./main

 */
